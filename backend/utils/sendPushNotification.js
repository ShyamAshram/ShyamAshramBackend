const sendPushNotification = async (userId, message) => {
    // Aquí iría la lógica para enviar notificaciones push
    console.log(`Notificación push enviada al usuario ${userId}: ${message}`);
  };
  
  module.exports = sendPushNotification;